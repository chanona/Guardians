#ifndef Enum_h__
#define Enum_h__

typedef enum Shader_Name
{
	SHADER_PLAYER,
	SHADER_TERRA,
	SHADER_BOW,
	SHADER_END,
}SHADER_NAME;

typedef enum Texture_Name
{
	TEXTURE_PLAYER,
	TEXTURE_MON,
	TEXTURE_END,
}TEXTURE_NAME;

typedef enum TexType_Type
{
	TEXTYPE_STATIC,
	TEXTYPE_DYNAMIC,
	TEXTYPE_END,
}TEX_TYPE;

#endif // Enum_h__
